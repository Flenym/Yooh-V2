export const OTP_TTL_MS = 5 * 60 * 1000;
export const DEFAULT_MONTH_LIMIT_BYTES = 20 * 1024 * 1024 * 1024;
export const DEFAULT_FILE_LIMIT_BYTES = 2 * 1024 * 1024 * 1024;
export const DEFAULT_FILE_RETENTION_DAYS = 30;
export const CLEANUP_INTERVAL_MS = 60 * 60 * 1000;

export const CHAT_TYPES = ["direct", "group", "channel", "server"];
export const CHAT_ROLES = ["owner", "admin", "member"];
